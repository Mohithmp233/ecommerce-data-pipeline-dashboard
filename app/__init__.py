"""Streamlit application package.

Top-level modules:
    app            : main Streamlit entry point (page config + tabs)
    tabs           : one module per application tab
    utils          : shared helpers (DB connector, chart builder)
"""
